# External empirical work remaining

The canonical software and protocol are complete, but empirical prerequisites
are intentionally absent from the repository:

- pinned FinQA, TAT-QA, and FinanceComplexQA source snapshots and exact hashes;
- a configured exact gateway chat-tokenizer endpoint for `gpt-5.4-mini`;
- a clean-commit preflight with exact model resolution and authoritative usage;
- outcome-blind development fit observations;
- complete operational pilot and independent blinded gate metrics;
- a passing non-overridable pilot gate;
- complete main gateway grid;
- hash-matching validation and analysis.

Until all prerequisites exist, no empirical conclusion is available and the
conditional hypothesis is neither supported nor cleanly falsified. The offline
fixture must never be promoted or copied into empirical artifacts.
